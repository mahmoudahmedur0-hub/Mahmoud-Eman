const intro=document.getElementById('intro');const site=document.getElementById('site');const openBtn=document.getElementById('openInvite');const canvas=document.getElementById('fx');const ctx=canvas.getContext('2d');let W=0,H=0,parts=[];function size(){W=canvas.width=innerWidth;H=canvas.height=innerHeight}addEventListener('resize',size);size();function particle(x,y,vx,vy,c,s,l,g=.04){parts.push({x,y,vx,vy,c,s,l,g,a:1})}function burst(x,y,n=90){const colors=['#ffe9ad','#f2c96d','#ffffff','#d49b36'];for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2;const sp=2+Math.random()*7;particle(x,y,Math.cos(a)*sp,Math.sin(a)*sp,colors[i%colors.length],2+Math.random()*4,70+Math.random()*50,.05)}}function rocket(){const x=W*.5,y=H*.9;let t=0;const id=setInterval(()=>{t++;particle(x,y-t*13,(Math.random()-.5)*.8,-5,'#ffe9ad',3,25,.01);if(t===40){clearInterval(id);burst(x,y-t*13,130)}},16)}function confetti(n=130){const colors=['#ffe9ad','#f2c96d','#fff4d6','#25d366'];for(let i=0;i<n;i++)particle(Math.random()*W,-20,Math.random()*2-1,2+Math.random()*4,colors[i%4],3+Math.random()*5,120+Math.random()*80,.03)}function loop(){ctx.clearRect(0,0,W,H);parts=parts.filter(p=>p.l-->0&&p.a>0);for(const p of parts){p.vy+=p.g;p.x+=p.vx;p.y+=p.vy;p.a=p.l/120;ctx.globalAlpha=Math.max(p.a,.12);ctx.fillStyle=p.c;ctx.beginPath();ctx.arc(p.x,p.y,p.s,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(loop)}loop();openBtn.addEventListener('click',()=>{intro.classList.add('open');rocket();setTimeout(()=>confetti(110),750);setTimeout(()=>{intro.classList.add('hide');site.classList.add('show');},1450)});const io=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));function updateTimers(){document.querySelectorAll('.count-card').forEach(card=>{let diff=new Date(card.dataset.target)-new Date();if(diff<0)diff=0;const d=Math.floor(diff/86400000);const h=Math.floor(diff%86400000/3600000);const m=Math.floor(diff%3600000/60000);const s=Math.floor(diff%60000/1000);card.querySelector('[data-days]').textContent=String(d).padStart(2,'0');card.querySelector('[data-hours]').textContent=String(h).padStart(2,'0');card.querySelector('[data-mins]').textContent=String(m).padStart(2,'0');card.querySelector('[data-secs]').textContent=String(s).padStart(2,'0')})}updateTimers();setInterval(updateTimers,1000);const celebrate=document.getElementById('celebrate');const thanks=document.getElementById('thanks');celebrate.addEventListener('click',()=>{thanks.classList.add('show');confetti(180);let c=0;const f=setInterval(()=>{burst(Math.random()*W,120+Math.random()*H*.38,75);if(++c>5)clearInterval(f)},350)});document.querySelector('.couple-img').addEventListener('error',e=>{e.currentTarget.alt='الصورة غير موجودة: تأكد أن couple.png بجوار index.html'});
